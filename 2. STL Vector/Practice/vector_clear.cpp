#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v = {10, 20, 30, 40, 50};

    // Clears all value from vector
    v.clear();

    // Print nothing
    for (int i : v)
    {
        cout << i << " ";
    }

    return 0;
}