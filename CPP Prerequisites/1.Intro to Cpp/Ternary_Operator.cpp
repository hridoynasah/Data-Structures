#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;

    // Ternary Operator  syntax:
    // (condition) ? True : False;
    x > y ? cout << x << endl : cout << y << endl;

    return 0;
}